#include <Stdio.h>

int main()
{

    int dayTemp[7] = {10, 12, 15, 8, 16, 5, 11};

    printf("Temperature of Monday: %dC \n", dayTemp[0]);
    printf("Temperature of Tueday: %dC \n", dayTemp[1]);
    printf("Temperature of Wednesday: %dC \n", dayTemp[2]);
    printf("Temperature of Thursday: %dC \n", dayTemp[3]);
    printf("Temperature of Friday: %dC \n", dayTemp[4]);
    printf("Temperature of Saturday: %dC \n", dayTemp[5]);
    printf("Temperature of Sunday: %dC \n", dayTemp[6]);

    return 0;
}